# Flask Configuration
SECRET_KEY=your-secret-key-here-change-in-production
DATABASE_URL=sqlite:///bonus_bot.db

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123

# Encryption Key for passwords (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
CIPHER_KEY=your-cipher-key-here

# Optional: External Database URL (for production)
# DATABASE_URL=postgresql://user:password@localhost/dbname